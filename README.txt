CS325 - Group 24 

INSTRUCTIONS FOR COMPILING AND RUNNING PROJECT 1 - MSS

Requirements and Notes:
 -Requires that MSS_Problems.txt be located in the working directory where the program is compiled. 
 - Results will be displayed to the console AND output to MSS_Results.txt* in the working directory. 
 - * NOTE THAT results will APPEND to MSS_Results.txt if the file already exists.  We did not want to risk deleting a file or data from another user.

1. Compile main.cpp using the command 'make'
2. Run the compiled program with the command './MSS'
3. remove program 'MSS' using the command 'make clean'

